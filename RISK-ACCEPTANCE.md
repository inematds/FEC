# RISK-ACCEPTANCE — Aceites de risco residual

> _⚠️ **Gerado de `evals/v1/risk-acceptance.json`.** NÃO edite manualmente — mudanças no MD são sobrescritas. Edite o JSON via PR (CODEOWNERS protege)._

Apenas `severity:high` em classes NÃO bloqueantes. Classes `security`, `data-integrity`, `sandbox`, `licensing`, `lab-breaking`, `injection-bypass` **não podem** estar aqui (PLAN item 74).

_Nenhum aceite de risco ativo._
